// Centralised asset URLs so we can swap models without grepping for paths.
// Models live in /public/models/ — Vite serves them at the site root.

export const CHARACTER_MODEL_URL = "/models/RobotExpressive.glb";
