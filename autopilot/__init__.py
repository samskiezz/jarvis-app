"""Repo-level autopilot — discover-first, propose-don't-mutate, organism-aware.

Reuses the assurance/ substrate (command bus, event bus, audit, invariants),
forge/ (approval queue), existing watchdogs (perception probes), Apollo/AIP/
Ontology services (subsystems), without duplicating them.

See AUTOPILOT.md for the operator entry point and assurance/SECURITY_INVARIANTS
for the safety guarantees.
"""
__version__ = "0.1.0"
