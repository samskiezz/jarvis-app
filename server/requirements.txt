fastapi>=0.115
uvicorn[standard]>=0.32
httpx>=0.27
pydantic>=2.6
python-multipart>=0.0.9
sse-starlette>=2.1
numpy>=1.26
scikit-learn>=1.3
scipy>=1.11
pytest>=8.0
# Scraping bundle — concurrent crawlers for the catalogue (see docs/PRODUCTION.md).
# Scrapling fetches with browser-TLS impersonation (beats 403/503 blocks); Scrapy
# is the large concurrent crawler. Both optional at runtime (degrade to the stdlib
# polite fetcher if absent).
scrapy>=2.13
scrapling>=0.4
curl_cffi>=0.7
browserforge>=1.2
cloudscraper>=1.2
# scrapling's fetchers module imports playwright at load time, so the HTTP fetcher
# (curl_cffi based, used by the document finder) needs the playwright PACKAGE present
# even though no browser is launched. Browser binaries are optional (setup.sh, for
# the stealth/JS fetchers only).
playwright>=1.40
# OCR (light path): pytesseract drives the system `tesseract` binary; PyMuPDF reads a
# PDF's text layer; pdf2image rasterises scanned pages. The heavy GPU OCR engines
# (easyocr/paddleocr) are installed by setup.sh, not here, to keep CI/image lean.
pytesseract>=0.3.10
PyMuPDF>=1.24
pdf2image>=1.17
Pillow>=10.0
