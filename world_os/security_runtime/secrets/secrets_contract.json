{
  "backend_options": [
    "Vault",
    "KMS",
    "cloud secret manager"
  ],
  "rules": [
    "no secrets in repository",
    "all connector credentials referenced by secret URI",
    "all secret reads audited",
    "rotation policy required per secret",
    "break-glass access requires approval and audit"
  ],
  "secret_reference_format": "secret://{environment}/{service}/{name}"
}