export type AnimatedToggleProps = { enabled: boolean; onChange: (enabled: boolean) => void; disabled?: boolean };
export function AnimatedToggle({ enabled, onChange, disabled }: AnimatedToggleProps) {
  return null; // Replace with Framer Motion ON/OFF sliding toggle.
}
