export type PipelineControlsProps = { id: string; enabled: boolean; status: string };
export function PipelineControls(props: PipelineControlsProps) {
  return null; // ON/OFF, Run, Pause, Stop, Re-run buttons call pipeline API.
}
